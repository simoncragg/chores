export type Chore = {
  id: string;
  name: string;
  isComplete: boolean;
  createdDate: string;
};

export type ChoreRecord = {
  id: string;
  name: string;
  is_complete: boolean;
  created_date: string;
};
